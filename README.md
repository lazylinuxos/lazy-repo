# lazy-repo
LazyLinux package repository

To use, create a file called /etc/xbps.d/99-repo.conf and add this line to it:
```
repository=https://github.com/lazylinuxos/lazy-repo/releases/download/v1.0
```